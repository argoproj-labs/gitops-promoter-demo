# Git repository URL used as spec.source.repoURL for the control-plane repo (Argo child apps, hydrator drySource for GitHub guestbook).
# Keep apps/root-app.yaml spec.source.repoURL identical.
repoURL: https://github.com/argoproj-labs/gitops-promoter-demo

# GitLab project that holds only the guestbook chart for the parallel GitLab demo (hydrator + Promoter operate here).
guestbookGitLabRepoURL: https://gitlab.com/gitops-promoter-group/gitops-promoter-project.git
