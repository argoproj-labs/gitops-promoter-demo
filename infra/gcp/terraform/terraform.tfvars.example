# Template for forks: copy to terraform.tfvars or edit the committed file, then:
# tofu init && tofu plan -out tfplan && tofu apply tfplan
#
# Region, cluster name, VPC CIDRs, and node sizing live as locals in main.tf.

project_id      = "my-unique-demo-project-id"
project_name    = "gitops-promoter-demo"
billing_account = "000000-000000-000000"
